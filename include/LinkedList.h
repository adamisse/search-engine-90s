/*#include "LLRBArch.h"
#include "Page.h"
#include <stdlib.h>

typedef struct lista Lista;

Lista* InicLista();

void InsereLista(Lista* , LLRBT *, char * );

Lista * procura(Lista*,char*);

int size(Lista*);

void printList(Lista *);

void free_list(Lista* lista);

void free_answer(Lista * lista);

void setPagesNames(Lista * l, Page ** page, int pageCount);*/